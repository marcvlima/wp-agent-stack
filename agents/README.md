# agents

Definições de subagentes especializados reutilizáveis entre projetos WordPress
(arquivos de agente com frontmatter `name`/`description`/`tools`). Ainda vazio —
promova um agente aqui quando ele se provar útil além do projeto de origem.
