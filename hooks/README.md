# hooks

Snippets de hooks (`settings.json`) e scripts de automação reutilizáveis entre projetos
WordPress (ex: lint automático pós-edit, checagens pré-commit específicas de WP/WooCommerce).
Ainda vazio — promova um hook aqui quando ele se provar útil além do projeto de origem.
